<%= form_with(model: rop) do |form| %>
  <% if rop.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rop.errors.count, "error") %> prohibited this rop from being saved:</h2>

      <ul>
        <% rop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :neighbourhood_id, style: "display: block" %>
    <%= form.text_field :neighbourhood_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
